@model IEnumerable<Library.Models.Prestamo>

@{
    ViewBag.Title = "Prestamos";
}

<div class="container mt-4">
    <h2 class="text-center mb-4"> Lista de Préstamos</h2>

    <p>
        @Html.ActionLink(" Crear Nuevo", "Create", null, new { @class = "btn btn-primary" })
    </p>

    <table class="table table-striped table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>@Html.DisplayNameFor(model => model.fecha_prestamo)</th>
                <th>@Html.DisplayNameFor(model => model.fecha_limite)</th>
                <th>@Html.DisplayNameFor(model => model.fecha_devolucion)</th>
                <th>@Html.DisplayNameFor(model => model.estado)</th>
                <th>@Html.DisplayNameFor(model => model.Cliente.nombre)</th>
                <th>@Html.DisplayNameFor(model => model.Copia.tipo)</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.fecha_prestamo)</td>
                    <td>@Html.DisplayFor(modelItem => item.fecha_limite)</td>
                    <td>@Html.DisplayFor(modelItem => item.fecha_devolucion)</td>
                    <td>
                        <span class="badge @(item.estado == "Devuelto" ? "bg-success" : "bg-warning text-dark")">
                            @Html.DisplayFor(modelItem => item.estado)
                        </span>
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.Cliente.nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.Copia.tipo)</td>
                    <td class="text-center">
                        @Html.ActionLink("Editar ", "Edit", new { id = item.id_prestamo }, new { @class = "btn btn-sm btn-warning" })
                        @Html.ActionLink("Detalles ", "Details", new { id = item.id_prestamo }, new { @class = "btn btn-sm btn-info" })
                        @Html.ActionLink("Borrar ", "Delete", new { id = item.id_prestamo }, new { @class = "btn btn-sm btn-danger" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <nav aria-label="Paginación de préstamos">
        <ul class="pagination justify-content-center">

            @if (ViewBag.CurrentPage > 1)
            {
                <li class="page-item">
                    @Html.ActionLink("«", "Index", new { page = ViewBag.CurrentPage - 1 }, new { @class = "page-link" })
                </li>
            }
            else
            {
                <li class="page-item disabled">
                    <span class="page-link">«</span>
                </li>
            }

            @for (int i = 1; i <= ViewBag.TotalPages; i++)
            {
                if (i == ViewBag.CurrentPage)
                {
                    <li class="page-item active">
                        <span class="page-link">@i</span>
                    </li>
                }
                else
                {
                    <li class="page-item">
                        @Html.ActionLink(i.ToString(), "Index", new { page = i }, new { @class = "page-link" })
                    </li>
                }
            }

            @if (ViewBag.CurrentPage < ViewBag.TotalPages)
            {
                <li class="page-item">
                    @Html.ActionLink("»", "Index", new { page = ViewBag.CurrentPage + 1 }, new { @class = "page-link" })
                </li>
            }
            else
            {
                <li class="page-item disabled">
                    <span class="page-link">»</span>
                </li>
            }

        </ul>
    </nav>
</div>